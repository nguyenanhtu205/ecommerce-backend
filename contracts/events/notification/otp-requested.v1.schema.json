{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "otp-requested.v1",
  "title": "OtpRequestedEvent",
  "description": "Published when the Auth Service generates an OTP code and the Notification Service needs to send it via email.",
  "type": "object",
  "required": [
    "email",
    "code",
    "purpose",
    "requestedAt"
  ],
  "additionalProperties": false,
  "properties": {
    "email": {
      "type": "string",
      "format": "email"
    },
    "code": {
      "type": "string",
      "description": "The OTP code in plaintext (included in the event only for email delivery and NEVER stored in plaintext by the Auth Service)."
    },
    "purpose": {
      "type": "string",
      "enum": [
        "register",
        "reset-password"
      ]
    },
    "requestedAt": {
      "type": "string",
      "format": "date-time"
    }
  }
}