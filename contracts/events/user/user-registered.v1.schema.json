{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "user-registered.v1",
  "title": "UserRegisteredEvent",
  "description": "Published when the Auth Service successfully creates a new user (after the set-password step).",
  "type": "object",
  "required": [
    "userId",
    "email",
    "registeredAt"
  ],
  "additionalProperties": false,
  "properties": {
    "userId": {
      "type": "string",
      "format": "uuid",
      "description": "The user's ID, used as the primary key for the profile row in the User Service."
    },
    "email": {
      "type": "string",
      "format": "email"
    },
    "registeredAt": {
      "type": "string",
      "format": "date-time",
      "description": "The time when the user completes registration (UTC, ISO 8601 format)."
    }
  }
}